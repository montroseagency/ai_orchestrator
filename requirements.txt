# Vibe Coding Team — Python Dependencies
watchdog>=4.0.0
# The thin launcher (vibe.py + src/) uses only the Python standard library.
# Optional session memory (cross-session learning) requires:
#   chromadb>=0.5.0
#   sentence-transformers>=3.0.0
# See also: src/rag_mcp/requirements.txt (for the MCP codebase search server)
